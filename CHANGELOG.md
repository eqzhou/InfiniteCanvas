# CHANGELOG

## Unreleased

+ [新增] 文本节点与文本配置节点支持低/中/高推理强度，并统一透传到 OpenAI Responses、Chat Completions 与服务端代理。
+ [新增] 图片/视频创作台支持拖放最多 10 个参考素材，按协议过滤类型、去重并进入实际生成请求。
+ [新增] 设置支持无密钥的配置与偏好 JSON 导入导出；导入写入数据库并保留当前账号已有密钥。
+ [调整] 移除旧画布助手入口，统一为可停靠的“画布 Agent”，保留 Codex/Claude、审批、附件、停止与生成任务状态。
+ [新增] 画布 Agent 提供只读、工作区自动执行（默认无网络）和完全访问三档 Codex 权限；权限会映射到真实 turn 级审批与沙箱策略。
+ [新增] 画布 Agent 增加可折叠过程时间线，合并工具/命令/文件/思考步骤的实时状态、增量详情和失败原因。
+ [优化] Agent 发送后立即清空输入并展示用户消息；新会话立即清屏；长时间运行显示耗时并保留停止当前 turn 操作。
+ [新增] 管理员站点策略：设置页可开关开放注册、自定义模型渠道与云端/后端代理生成；注册与后端生成接口按策略强制。
+ [新增] AI 调用日志：后端代理生成任务记录请求/响应摘要、耗时、模型与渠道；管理员可在 /ai-logs 浏览、筛选、按条删除与按天数清理；密钥与二进制内容脱敏。
+ [新增] AI 调用详情显示实际 HTTP 接口；图生图日志记录有序参考图存储键和基础元数据，便于定位输入图片，同时不保存图片二进制或凭据。
+ [新增] 服务器素材库：租户级 URL/文本素材目录，用户可浏览/插入画布，管理员可维护；提供 /api/library-assets CRUD。
+ [新增] 双击画布空白菜单支持上传资源与从素材库插入，并按双击位置落点。
+ [新增] 图片节点连接到导演台后可作为环境源选择；普通图片按普通环境背景，严格 2:1 全景节点仍作为球形环境。
+ [新增] 本地上传/拖入严格 2:1 图片时，可选择作为全景图导入或作为普通图片导入；Codex 附件仍强制普通图片节点。
+ [修复] 连续指针操作后画布拖动状态清理：节点拖拽/缩放/连线也在画布表面 setPointerCapture，并在 pointerup/cancel/lostpointercapture/Escape 时统一释放，避免 pan 卡住。
+ [优化] 节点模型名过长时的控件布局：文本/配置节点模型输入 truncate + min-w-0，提示词库选择固定宽度 shrink-0；悬浮工具栏与节点提示词条收窄最大宽度，避免与发送按钮重叠。
+ [新增] 设置中的用户级 S3/R2 对象存储配置接入受保护媒体读写：启用后按租户优先于进程级 OPENBOARD_S3_*，凭据仍仅保存在加密 secrets；无效配置 fail-closed，关闭后回退进程级/本地文件系统。
+ [修复] 生成历史删除改为软删除：默认列表隐藏已删记录，保留 tombstone 供多设备同步；按项目清理时仍硬删除。
+ [新增] 图片/视频创作台生成历史支持多选与批量删除；服务端提供有界 bulk-delete 接口。
+ [修复] 多标签页保存画布项目时不再隐式删除其他标签页中的项目；删除仅发生在用户显式删除或完整工作区替换。
+ [修复] 删除画布项目或图片/视频/音频节点时，同步清理对应生成任务历史，且不影响其他画布项目。
+ [修复] 画布节点提示词框粘贴多行文本时保留换行与空行，并支持在提示词内滚轮滚动而不缩放画布。
+ [新增] 视频创作台、配置节点与视频节点支持首尾帧参考模式；Ark/Seedance 按图片顺序映射 `first_frame` / `last_frame`，并在重试与持久化任务中保留。
+ [新增] 提示词来源自定义脚本支持 async 抓取（fetchText/fetchJson）与本机/局域网源 URL。
+ [新增] 提示词中心增加 Tiger Xianyu GPT Image 2 结构化 Markdown 目录，支持嵌套分类、H5 编号补充提示词、稳定条目 ID、管理员 Markdown 来源同步与自定义格式选择。
+ [新增] 图片工作台、配置节点与设置统一提供 1:1、3:2、2:3、4:3、3:4、16:9、9:16、21:9、5:4、4:5 尺寸预设，并按已知模型能力筛选尺寸、质量/分辨率和输出数量；请求前会统一归一化旧比例、质量和数量。
+ [优化] 画布侧栏 Tab 下划线滑动动画。

+ [新增] 提示词中心对齐 Image Prompts 统一 JSON 目录与「我的提示词」。
+ [新增] 可选 GA4 / 百度统计（环境变量注入，默认关闭）。
+ [新增] 顶栏版本更新弹窗与更新日志时间线。
+ [新增] 画布侧栏素材支持上传图片/视频。

## v0.1.0 - 2026-07-21

+ [新增] OpenBoard 独立 clean-room 无限画布实现，覆盖画布、工作台、Agent、插件与提示词中心。
